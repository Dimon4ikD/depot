.page-header

%h1 Элемент корзины
-if @line_items.blank?
  .alert.alert-info В корзин нет товаров
-else
  .list-group
    -@line_items.each_with_index do |line_item, i|
      .list-group-item
        .col-md-2
          %span.text-danger="#{i+1}"
          =line_item.product.name

        .col-md-3
          =line_item.quantity
          &times;
        .col-md-3
          =number_to_currency line_item.price
          ="= #{number_to_currency (line_item.total_price)}"
          -#=line_item.price


        .clearfix
  .alert.alert-info
    Всего
    =@line_items.to_a.sum(&:quantity)
    #{Russian.p(@line_items.to_a.sum(&:quantity),'товар','товара','товаров')} на сумму
    =number_to_currency @line_items.to_a.sum(&:total_price)


-#%table
-#  %thead
-#    %tr
-#      %th Количество
-#      %th Цена
-#      %th Корзина
-#      %th Товар
-#      %th
-#      %th
-#      %th
-#
-#  %tbody
-#    - @line_items.each do |line_item|
-#      %tr
-#        %td= line_item.quantity
-#        %td= line_item.price
-#        %td= line_item.cart.id
-#        %td= line_item.product.name
-#        %td= link_to 'Показать', line_item
-#        %td= link_to 'Изменить', edit_line_item_path(line_item)
-#        %td= link_to 'Удалить', line_item, :method => :delete, :data => { :confirm => 'Вы уверены?' }
-#
-#%br

= link_to 'Новый элемент корзины', new_line_item_path, class: "btn btn-info"
= link_to 'Очистить корзину', @cart, data:{method: :delete, confirm:"Вы уверены?"}, class: "btn btn-danger"